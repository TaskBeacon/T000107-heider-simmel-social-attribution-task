# Task Plot Review

## Round 1 — regenerate

Evidence match:

- PASS: exactly three rows (`Social`, `Mechanical`, `Random`) and five aligned participant-visible phases.
- PASS: phase order, response options, main durations, no-feedback design, and blank header band are correct.
- PASS: two triangles and one circle use the intended color/size hierarchy.
- FAIL: in all animation snapshots, the outlined enclosure is drawn as if it were the full participant-screen border. The real stimulus has a smaller right-side enclosure with clear white margins on every side.
- FAIL: the mechanical snapshot adds a second nearly full-screen gray wall frame rather than a compact rail/wall system plus the separate right-side enclosure.
- NEEDS CLARITY: `Animation ×2` shows `15 s each` but omits the 0.5 s fixation between playbacks.

Visual quality:

- PASS: text is readable, arrows and rows are clear, no overlap, no decorative content, no generated title/logo/watermark.

Targeted revision request:

1. In every animation snapshot, draw a small dark outlined rectangular enclosure only on the right third of the screen, with white margin around it and a doorway gap on its left edge; it must not touch or replace the participant-screen border.
2. In the mechanical row, use only one compact gray rail/wall element in the left/middle area and retain the same small right-side enclosure; add the compact sublabel `0.5 s between` beneath `15 s each` in all three rows.

## Round 2 — pass

Evidence match:

- PASS: the diagram retains exactly three comparison rows and the canonical five participant-visible phases.
- PASS: every animation panel now contains a small right-side enclosure with surrounding whitespace and a doorway gap.
- PASS: the mechanical panel uses a compact rail/wall element that is visually distinct from the enclosure.
- PASS: the two 15 s playbacks and the 0.5 s fixation between them are explicit in every row.
- PASS: narrative and family-judgment response ordering, response labels, timeouts, and ITI match the canonical task.

Visual quality:

- PASS: no text, connector, or panel overlap; the three rows and five columns align cleanly.
- PASS: stimulus scale and aspect are plausible, with two triangles and one circle clearly distinguishable.
- PASS: the standard TaskBeacon header and logo occupy only the reserved header band.
- PASS: no misleading feedback, score, decorative scene, or copied Heider-Simmel film frame is shown.

Decision: accepted after visual iteration 2 of 5 maximum.
